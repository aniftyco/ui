---
title: Getting Started
---

import { Tabs, Tab } from '../../components/Tabs';

## Installation

<Tabs items={['npm', 'yarn', 'pnpm']} storageKey="selected-pkg-manager">
  <Tab>
    ```sh
    npm install nifty-ui --save
    ```
  </Tab>
  <Tab>
    ```sh
    yarn add nifty-ui
    ```
  </Tab>
  <Tab>
    ```sh
    pnpm install nifty-ui --save
    ```
  </Tab>
</Tabs>